//Nome de variáveis
    //Modo incorreto
        const primeironome = "João"
        const segundonome = "Santos"
    
    //Modo certo
        const primeiroNome = "João" 
        const segundoNome = "Santos" 

//Nome de função
    //Modo incorreto
        function mostrardata(){ 
            console.log (horario);
        }
        mostrardata();

    //Modo certo
    function mostrarData() {
        console.log(Date());
    }

    mostrarData();

//Nome de classe
    //Modo incorreto
        class cliente {

        }

    //Modo certo
        class Cliente {
            
        }











